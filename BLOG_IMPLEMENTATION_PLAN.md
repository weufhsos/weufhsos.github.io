# GitHub.io 个人博客重构实施方案

> 版本：v1.0  
> 日期：2026-09-04  
> 状态：实施中（首个可识别版本已完成）  
> 目标站点：<https://weufhsos.github.io/>

## 1. 决策摘要

本项目不继续直接修改现有的 Hexo 生成文件，而是在保留 GitHub Pages 托管方式的前提下，将网站重构为一个轻量、可维护、有个人辨识度的“个人主页 + 技术博客 + 项目作品集”。

推荐方案：

- 使用 Astro 构建静态网站。
- 使用 Markdown/MDX 编写文章。
- 使用 GitHub Actions 自动构建并发布到 GitHub Pages。
- 首页突出个人定位、当前研究方向、精选项目和最新文章。
- 保留 Giscus 评论，移除随机背景、访问量组件和无必要的主题功能。
- 优先保证内容、阅读体验、移动端、性能和无障碍，不追求堆叠动画与组件。

预期结果：访客在 5 秒内理解“你是谁、你在做什么、这里有什么值得阅读”，作者可以通过新增一篇 Markdown 文件完成日常发布。

## 2. 当前基线

### 2.1 已发现的问题

- 仓库只有生成后的 HTML、CSS 和 JavaScript，没有 Hexo 源工程、文章源文件和主题配置。
- 站名仍为 `My Blog`，简介和公告接近模板默认内容。
- 首页使用 Picsum 随机全屏图片，刷新后视觉风格不稳定。
- 首页首屏几乎看不到文章或项目，移动端图片裁切不可控。
- 头像地址使用无效的 GitHub 用户 ID，占位图被当作正式头像展示。
- 只有一篇 `Hello World`，但页面同时展示大量分类、标签、归档、公告和访问统计。
- 缺少 README、自动构建流程、RSS、sitemap、robots.txt 和独立站点图标配置。
- 首页首次加载依赖多个第三方服务和整套主题资源。
- 日期、分类和页脚等小字号文字存在对比度不足。

### 2.2 测量基线

2026-09-04 的一次移动端 Lighthouse 实验室测量结果：

| 指标 | 当前结果 | 首版目标 |
|---|---:|---:|
| Performance | 83 | ≥ 95 |
| Accessibility | 94 | ≥ 95，争取 100 |
| Best Practices | 77 | ≥ 95 |
| SEO 基础检查 | 100 | 保持 100，并补齐站点级能力 |
| LCP | 3.5 秒 | ≤ 2.5 秒 |
| CLS | 0.004 | ≤ 0.1 |
| 首次传输体积 | 约 583 KiB | ≤ 400 KiB，不含文章主动加载图片 |

说明：Lighthouse 单次结果会受网络和远程图片影响，最终应以多次测试和真实用户数据为准。

## 3. 实施假设

除非实施前另行确认，首版按以下假设推进：

- 主要语言为简体中文。
- 显示名称暂用 `adand`，账号标识保留 `weufhsos`。
- 内容方向暂定为 AI Agent、自动化工具、Python/Go 工程实践与研究记录。
- 网站继续部署在 `weufhsos.github.io`，首版不强制购买独立域名。
- 公开项目优先展示 `GenericAgent`、`AutoResearch` 和 `adaptive_sync_go`。
- 不引入后台、数据库、登录系统或在线 CMS。
- Giscus 作为唯一评论系统，仅在文章页按需加载。
- 现有 `Hello World` 不作为首页推荐内容；根据需要归档或保留兼容地址。

如果实际个人定位与上述内容不同，应先修改文案和信息架构，再进入视觉细化。

## 4. 项目目标与非目标

### 4.1 项目目标

1. 建立明确、可信的个人品牌和技术定位。
2. 同时承载技术文章、项目展示和个人介绍。
3. 让桌面端和移动端都具有稳定、克制的视觉体验。
4. 让文章以 Markdown/MDX 管理，并在提交后自动发布。
5. 提供完整的页面元数据、分享卡片、RSS 和 sitemap。
6. 减少第三方依赖，达到明确的性能和无障碍指标。
7. 保留可回滚的旧站版本，避免重构过程中丢失现有内容。

### 4.2 首版非目标

- 用户注册、登录和权限系统。
- 数据库、服务端接口和动态管理后台。
- 多语言站点。
- PWA、离线应用和消息推送。
- 复杂搜索；文章不足 10 篇时不引入全文搜索。
- 访问量、在线人数、运行时间等装饰性统计。
- 大量粒子、视差、鼠标跟随或页面切换特效。

## 5. 信息架构

### 5.1 首版路由

| 路由 | 页面 | 主要目的 |
|---|---|---|
| `/` | 首页 | 说明身份，展示精选项目和最新文章 |
| `/posts/` | 文章列表 | 按时间浏览文章，可按主题筛选 |
| `/posts/[slug]/` | 文章详情 | 提供专注、舒适的长文阅读体验 |
| `/projects/` | 项目列表 | 展示代表性项目、状态和技术方向 |
| `/projects/[slug]/` | 项目详情 | 解释问题、方案、贡献和成果 |
| `/about/` | 关于 | 完整个人介绍、能力、关注方向与联系方式 |
| `/rss.xml` | RSS | 允许读者订阅文章 |
| `/404.html` | 404 | 提供返回首页和文章页的出口 |

### 5.2 导航

桌面端主导航：

- 文章
- 项目
- 关于
- GitHub

移动端使用紧凑菜单，但核心入口必须可以通过键盘和触摸操作。首页链接由站点名称承担，不重复显示“首页”按钮。

当文章数量超过 10 篇后，再评估添加搜索和独立标签页。

## 6. 页面设计

### 6.1 首页

首页从上到下分为五个区域：

1. 顶部导航：站点名称、主要入口、主题切换。
2. 个人介绍：姓名、具体定位、两句介绍、文章和项目按钮。
3. 当前关注：简短说明最近在研究或构建什么。
4. 精选项目：展示 3 个项目，每个项目包含一句问题描述、技术标签和链接。
5. 最新文章：展示 3–6 篇文章，包含标题、摘要、日期和阅读时间。

首屏要求：

- 不使用随机远程背景图。
- 高度不超过约 `min(760px, 85vh)`。
- 在常见笔记本屏幕上至少露出下一区域的开始部分。
- 文案是首屏主角，图像只承担辅助作用。

### 6.2 文章列表

- 默认按发布日期倒序排列。
- 每篇展示标题、摘要、日期、阅读时间和最多 3 个标签。
- 不使用高度一致但内容空洞的卡片网格，优先使用清楚的编辑式列表。
- 草稿文章在生产构建中排除。
- 分页在文章超过 20 篇后再实现。

### 6.3 文章详情

- 正文最大宽度 720–760px。
- 正文字号 17–18px，行高约 1.75。
- 标题区展示标题、摘要、发布日期、更新时间和阅读时间。
- 长文章在桌面端提供粘性目录；移动端提供可展开目录。
- 代码块提供语言标识和复制按钮。
- 图片必须提供替代文本和明确尺寸，避免布局跳动。
- 文末展示标签、相关文章和 Giscus 评论。
- 移除浏览量、全套社交分享按钮和版权信息大卡片；保留简洁的许可说明和复制链接。

### 6.4 项目页面

每个项目详情按统一叙事结构组织：

1. 项目解决的问题。
2. 背景和约束。
3. 个人承担的工作。
4. 核心方案与关键取舍。
5. 结果、截图或可验证产出。
6. GitHub、演示或相关文章链接。

项目卡片不能只展示仓库名和编程语言。

### 6.5 关于页面

建议内容：

- 80–150 字个人介绍。
- 关注的技术方向。
- 代表性经历或工作方式。
- 当前正在学习或构建的内容。
- GitHub、邮箱及其他主动提供的联系方式。

不展示未经本人确认的履历、公司、学校或地理信息。

## 7. 视觉系统

### 7.1 设计方向

采用“编辑式开发者笔记”风格：清晰排版、少量强调色、宽松留白、低装饰噪音。视觉个性来自文字、节奏和内容，而不是通用风景照片。

### 7.2 建议色彩

浅色模式：

| 用途 | 建议值 |
|---|---|
| 页面背景 | `#F5F3EE` |
| 内容表面 | `#FFFDFC` |
| 主文字 | `#18181B` |
| 次文字 | `#5B5B61` |
| 强调色 | `#315CF4` |
| 边框 | `#DCD8CF` |

深色模式：

| 用途 | 建议值 |
|---|---|
| 页面背景 | `#111315` |
| 内容表面 | `#181B1F` |
| 主文字 | `#EEF0F2` |
| 次文字 | `#A7ADB5` |
| 强调色 | `#8AA4FF` |
| 边框 | `#30343A` |

颜色最终必须通过 WCAG AA 对比度检查。强调色不能作为唯一状态提示。

### 7.3 字体与排版

- 首版优先使用系统字体栈，避免远程字体阻塞首屏。
- 中文字体顺序建议包含 `PingFang SC`、`Microsoft YaHei` 和系统无衬线字体。
- 代码使用系统等宽字体。
- 标题不使用大面积全大写或过重阴影。
- 正文每行约 35–45 个中文字符。

### 7.4 动效

- 只保留导航、按钮、卡片和主题切换的短过渡。
- 常规过渡控制在 120–220ms。
- 不使用打字机循环动画作为核心信息。
- 对 `prefers-reduced-motion: reduce` 关闭非必要动画。

### 7.5 图像原则

- 头像或个人标识保存为站点本地资源。
- 项目图片使用真实截图，不使用无关图库照片。
- 文章图片在构建时生成响应式尺寸和 WebP/AVIF。
- 首页最多使用一个主要视觉元素。
- 品牌文案稳定后制作一张 1200×630 的站点级社交分享图。

## 8. 技术架构

### 8.1 核心技术

- Astro：静态页面生成与路由。
- TypeScript：配置和内容数据约束。
- Markdown/MDX：文章和项目内容。
- Astro Content Collections：文章、项目元数据校验。
- `@astrojs/sitemap`：自动生成 sitemap。
- `@astrojs/rss`：生成 RSS。
- Giscus：文章评论。
- GitHub Actions：构建并部署到 GitHub Pages。

首版不引入 React、Vue、Tailwind 或全局状态管理；只有确实需要交互的局部功能使用少量原生 JavaScript。

### 8.2 建议目录

```text
/
├─ .github/
│  └─ workflows/
│     └─ deploy.yml
├─ public/
│  ├─ favicon.svg
│  ├─ favicon.ico
│  ├─ og.png
│  ├─ robots.txt
│  └─ legacy/
├─ src/
│  ├─ assets/
│  │  ├─ avatar.*
│  │  ├─ posts/
│  │  └─ projects/
│  ├─ components/
│  │  ├─ Header.astro
│  │  ├─ Footer.astro
│  │  ├─ ThemeToggle.astro
│  │  ├─ PostListItem.astro
│  │  ├─ ProjectCard.astro
│  │  ├─ TableOfContents.astro
│  │  └─ Comments.astro
│  ├─ content/
│  │  ├─ posts/
│  │  └─ projects/
│  ├─ layouts/
│  │  ├─ BaseLayout.astro
│  │  └─ PostLayout.astro
│  ├─ pages/
│  │  ├─ index.astro
│  │  ├─ about.astro
│  │  ├─ 404.astro
│  │  ├─ posts/
│  │  ├─ projects/
│  │  └─ rss.xml.js
│  ├─ styles/
│  │  ├─ global.css
│  │  └─ prose.css
│  ├─ content.config.ts
│  └─ consts.ts
├─ astro.config.mjs
├─ package.json
├─ tsconfig.json
└─ README.md
```

结构允许按实现细节小幅调整，但不为单次使用场景创建额外抽象。

### 8.3 文章数据模型

```yaml
title: 文章标题
description: 一至两句摘要
pubDate: 2026-09-04
updatedDate: 2026-09-04 # 可选
tags:
  - AI Agent
  - Python
draft: false
featured: false
heroImage: ./cover.webp # 可选
heroAlt: 图片内容说明 # heroImage 存在时必填
```

构建时校验规则：

- 标题、摘要和发布日期必填。
- `description` 建议为 50–160 个中文字符。
- 标签去重，首版每篇最多 5 个。
- 封面存在时必须提供 `heroAlt`。
- `draft: true` 的内容不进入生产页面、RSS 和 sitemap。

### 8.4 项目数据模型

```yaml
title: GenericAgent
description: 用一句话描述它解决的问题
status: active
featured: true
languages:
  - Python
repoUrl: https://github.com/weufhsos/GenericAgent
demoUrl: null
order: 1
```

## 9. SEO 与分享

### 9.1 全站元数据

- 使用真实站点名称和描述。
- 首页 canonical 为 `https://weufhsos.github.io/`。
- 统一使用不带 `index.html` 的规范链接。
- 配置 favicon、theme-color、RSS 自动发现和 sitemap 入口。
- 首页使用唯一一张站点级 `og.png`。
- 添加 `WebSite` 和 `Person` 结构化数据。

### 9.2 文章元数据

- 标题、描述、发布日期和分享内容来自同一篇文章的 frontmatter。
- 文章有封面时使用该封面作为 Open Graph 和 X 图片。
- 文章没有封面时明确省略图片，不误用首页的通用分享图。
- 添加 `BlogPosting` 结构化数据。
- 发布和更新时间必须与页面显示值一致。

### 9.3 搜索引擎入口

- 生成 sitemap。
- 添加 robots.txt 并声明 sitemap 地址。
- 部署后验证 Google Search Console。
- 如果主要读者位于中国大陆，可再评估百度搜索资源平台。

## 10. 性能、隐私与无障碍

### 10.1 性能预算

- 首页不依赖外部随机大图。
- 首屏关键图片必须指定尺寸并优化格式。
- 首版页面尽量不发送客户端框架代码。
- 不全站加载评论、代码高亮或灯箱资源。
- 不引入完整图标字体；优先使用文本或按需图标。
- 首页总 JavaScript 建议控制在 30 KiB gzip 以内。
- 图片以下内容默认懒加载，首屏主图不得懒加载。

### 10.2 第三方服务

保留：

- GitHub 链接。
- 文章页按需加载的 Giscus。

移除：

- Picsum 随机图片。
- 不蒜子访问统计。
- 全站 Fancybox、Typed.js 和 Share.js。
- 无实际用途的 CDN 资源。

分析统计首版默认不启用。如果后续确有需求，单独选择隐私友好且体积较小的方案。

### 10.3 无障碍要求

- 正常文字对比度至少 4.5:1。
- 所有交互可通过键盘访问。
- 焦点状态清晰可见，不能仅依赖颜色。
- 图标按钮必须有可读标签。
- 图片提供准确的替代文本；纯装饰图片使用空替代文本。
- 标题层级连续，每页只有一个主要 `h1`。
- 支持放大到 200% 仍可阅读和操作。
- 尊重减少动态效果和系统深浅色偏好。

## 11. 内容准备

### 11.1 首发最小内容

- 1 份真实的个人介绍。
- 3 个项目条目，其中至少 1 个有完整项目详情。
- 3 篇能代表方向的文章或文章草稿。
- 1 个头像或文字标识。
- GitHub 和至少一种本人确认的联系方式。

### 11.2 建议首批文章

1. `GenericAgent`：目标、架构与关键取舍。
2. `AutoResearch`：研究工作流如何设计和自动化。
3. `adaptive_sync_go`：实现过程中遇到的问题与解决方案。
4. 旧站重构复盘：为什么从默认博客主题转向个人知识作品集。

文章优先写真实项目经验，不写泛化的“如何安装框架”类模板内容。

## 12. 迁移与部署策略

### 12.1 安全迁移

实施前：

1. 确认当前远程 `main` 与工作区一致。
2. 为现有站点创建可恢复的 tag 或归档分支，例如 `legacy-hexo-2026-09`。
3. 记录 GitHub Pages 当前发布来源。
4. 再将 `main` 转换为 Astro 源工程。

任何时候都不得在没有备份的情况下直接删除旧站文件。

### 12.2 自动部署

- `main` 保存可阅读、可构建的源代码。
- GitHub Actions 在每次推送到 `main` 时安装依赖、构建静态文件并发布 Pages artifact。
- 不再手动提交 `dist/`。
- 工作流使用 GitHub Pages 官方权限和 `github-pages` environment。
- README 记录本地预览、写文章和发布方式。

### 12.3 旧链接

当前文章地址：

```text
/2026/01/20/hello-world/
```

处理方式按以下优先级：

1. 如果保留文章，则继续生成同一路径。
2. 如果改为新路径，则在旧路径保留静态跳转页并设置新 canonical。
3. 如果确认该模板文章没有保留价值，可以删除，但仍保留友好的旧地址提示页。

## 13. 分阶段实施计划

### 阶段 0：保护与准备

任务：

- [x] 备份现有站点并建立恢复点。
- [x] 初始化 Astro 源工程。
- [x] 配置站点 URL 和 GitHub Pages 构建基础。
- [x] 建立文章和项目内容模型。

完成标准：本地能构建空的正式站点骨架，旧站可以恢复。

### 阶段 1：首个可识别版本

任务：

- [x] 完成全局色彩、排版和页面布局。
- [x] 完成导航和首页首屏。
- [x] 使用暂定身份文案和至少一个真实项目。
- [x] 完成桌面及移动端首屏。

完成标准：首次预览时，访客能够看出这是 `adand/weufhsos` 的个人技术网站，而不是通用模板。

### 阶段 2：核心页面

任务：

- [x] 完成首页所有区域。
- [x] 完成文章列表和文章详情模板。
- [x] 完成项目列表和项目详情模板。
- [x] 完成关于页、页脚和 404 页面。
- [x] 接入首版示例内容（上线前需替换为本人最终文案）。

完成标准：四类核心页面可正常访问，移动端没有横向溢出或内容遮挡。

### 阶段 3：传播与质量能力

任务：

- [x] 配置页面级 title、description、canonical 和结构化数据。
- [ ] 创建并验证站点级社交分享图。
- [x] 配置 RSS、sitemap 和 robots.txt。
- [x] 按需加载 Giscus。
- [x] 添加深色模式和减少动态效果支持。

完成标准：首页和代表性文章分享时显示正确标题、摘要和图片，RSS 与 sitemap 可访问。

### 阶段 4：验证与发布

任务：

- [x] 执行完整构建和类型检查。
- [ ] 检查内部链接、旧地址和 404。
- [ ] 检查桌面与手机布局。
- [ ] 检查键盘导航、焦点状态和文字对比度。
- [ ] 检查首页与至少两篇内容页的元数据。
- [ ] 执行 Lighthouse 测量并修复主要回归。
- [ ] 通过 GitHub Actions 发布。
- [ ] 发布后再次进行线上抽查。

完成标准：满足第 14 节的验收标准，并确认可以从归档版本恢复旧站。

## 14. 验收标准

### 14.1 功能

- [ ] 首页、文章、项目、关于和 404 页面正常访问。
- [ ] 导航、主题切换、目录、复制代码和评论按预期工作。
- [ ] 文章和项目由内容文件生成，不手工复制页面 HTML。
- [ ] 草稿不会发布。
- [ ] 推送到 `main` 后自动完成 GitHub Pages 部署。

### 14.2 视觉与响应式

- [ ] 375px、768px、1280px 和 1440px 宽度下无横向滚动。
- [ ] 首页首屏不被随机图片占满，并能看到有效内容入口。
- [ ] 正文宽度、字号、行高和代码块在手机上可读。
- [ ] 深浅色模式均无明显对比度问题。
- [ ] 页面视觉在刷新后保持一致。

### 14.3 SEO 与分享

- [ ] 每个可分享页面有独立 title 和 description。
- [ ] canonical 使用正式、干净的绝对 URL。
- [ ] 首页与文章的 Open Graph/X 信息和可见内容一致。
- [ ] 无封面的文章不误用不相关图片。
- [ ] RSS、sitemap 和 robots.txt 可以访问。
- [ ] favicon 和站点图标正确显示。

### 14.4 性能与无障碍

- [ ] Lighthouse Performance ≥ 95。
- [ ] Lighthouse Accessibility ≥ 95。
- [ ] Lighthouse Best Practices ≥ 95。
- [ ] Lighthouse SEO = 100。
- [ ] LCP ≤ 2.5 秒，CLS ≤ 0.1。
- [ ] 普通文字对比度至少 4.5:1。
- [ ] 仅使用键盘可以访问所有核心功能。

说明：上述指标以生产构建、稳定网络和多次测试中的合理结果为准，不为了单一分数牺牲真实阅读体验。

## 15. 风险与应对

| 风险 | 影响 | 应对方式 |
|---|---|---|
| Hexo 源工程已丢失 | 旧站无法重新生成 | 将现有静态文件归档，只迁移仍有价值的内容 |
| 个人定位尚未确定 | 首页文案反复修改 | 阶段 1 前确认姓名、方向和一句话介绍 |
| 内容不足 | 新设计仍显空洞 | 至少准备 3 个项目和 3 篇文章再正式发布 |
| 更换 URL | 旧链接失效 | 保留原路径或添加静态跳转页 |
| 第三方服务不稳定 | 评论或页面变慢 | 评论按需加载，核心内容不依赖第三方脚本 |
| 视觉过度设计 | 阅读性和性能下降 | 使用固定设计系统，并以验收指标限制范围 |

## 16. 实施前需要确认的资料

以下信息缺失时可以先用占位内容开发，但上线前必须确认：

- 网站显示名称：`adand`、`weufhsos` 或其他名称。
- 一句话定位和 80–150 字个人介绍。
- 希望重点展示的 3 个项目。
- 头像、照片、文字标识或“不使用头像”的选择。
- 公开联系方式和社交链接。
- 内容是否只用中文。
- 现有 Hexo 源工程是否还保存在其他位置。
- `Hello World` 是保留、归档还是删除。
- 是否计划购买独立域名。

## 17. 预期交付物

- 完整 Astro 源工程和依赖锁文件。
- 首页、文章、项目、关于和 404 页面。
- 可校验的文章与项目内容模型。
- 至少一篇示例文章和三个项目条目。
- 响应式浅色/深色视觉系统。
- favicon 和站点级社交分享图。
- RSS、sitemap、robots.txt 和结构化数据。
- GitHub Actions 自动部署流程。
- README 使用说明。
- 发布前验证记录和可恢复的旧站归档。

## 18. 参考资料

- GitHub Pages 创建与静态站点生成器：<https://docs.github.com/en/pages/getting-started-with-github-pages/creating-a-github-pages-site>
- GitHub Pages 自定义工作流：<https://docs.github.com/en/pages/getting-started-with-github-pages/using-custom-workflows-with-github-pages>
- Astro 部署到 GitHub Pages：<https://docs.astro.build/en/guides/deploy/github/>
- Astro Content Collections：<https://docs.astro.build/en/guides/content-collections/>
- Astro 图片优化：<https://docs.astro.build/en/guides/images/>
- Astro RSS：<https://docs.astro.build/en/recipes/rss/>
- Astro Sitemap：<https://docs.astro.build/en/guides/integrations-guide/sitemap/>
- Google SEO Starter Guide：<https://developers.google.com/search/docs/fundamentals/seo-starter-guide>
- Web Vitals：<https://web.dev/articles/vitals>
- WCAG 2.2 Quick Reference：<https://www.w3.org/WAI/WCAG22/quickref/>
